#include <stdio.h>
#include "main.h"

/**
 * mul - mutiply two integers
 * @b: para1
 * @a: para2
 * Return: product of a and b
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
